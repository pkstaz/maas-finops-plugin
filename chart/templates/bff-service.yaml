{{- if .Values.bff.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "maas-finops.fullname" . }}-bff
  namespace: {{ include "maas-finops.namespace" . }}
  labels:
    {{- include "maas-finops.labels" . | nindent 4 }}
    app.kubernetes.io/component: bff
spec:
  type: {{ .Values.bff.service.type }}
  ports:
    - port: {{ .Values.bff.service.port }}
      targetPort: {{ .Values.bff.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "maas-finops.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: bff
{{- end }}
